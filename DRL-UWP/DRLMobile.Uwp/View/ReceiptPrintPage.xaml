<Page
    x:Class="DRLMobile.Uwp.View.ReceiptPrintPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DRLMobile.Uwp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="PrintableArea">
        <ScrollViewer Margin="12,20,12,12" ZoomMode="Enabled">
            <StackPanel>
                <StackPanel x:Name="Header" Visibility="Collapsed">
                    <TextBlock TextWrapping="Wrap" FontSize="14" FontWeight="Normal"/>
                    <!--<StackPanel x:Name="RenderingPanel" Visibility="Visible">
                        <Image x:Name="Output" AutomationProperties.Name="PDF document content" Stretch="None" Margin="0,10,0,0"/>
                    </StackPanel>-->

                    <ItemsControl ItemsSource="{x:Bind PdfPages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Image x:Name="Output" Source="{Binding}" Stretch="None" Margin="10" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                
                <RichTextBlockOverflow x:Name="ContinuationPageLinkedContainer"/>
                
                <StackPanel x:Name="Footer" Visibility="Collapsed">
                    <TextBlock Foreground="Black" FontSize="16" TextAlignment="Left" FontFamily="Segoe UI">
                        Copyright © Republic Brands, LLC.
                    </TextBlock>
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
