<UserControl
    x:Class="DRLMobile.Uwp.CustomControls.DashboardButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DRLMobile.Uwp.CustomControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Button
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="Transparent"
        Command="{Binding ButtonClick, ElementName=root}"
        PointerEntered="Button_PointerEntered"
        PointerExited="Button_PointerExited">
        <Grid>
            <StackPanel Spacing="10">
                <Image
                    x:Name="ButtonImage"
                    Width="{x:Bind ImageHeightWidth, Mode=OneWay}"
                    Height="{x:Bind ImageHeightWidth, Mode=OneWay}"
                    Source="{x:Bind CurrentImgSource, Mode=OneWay}"
                    />
                <TextBlock
                    Name="Title"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="{Binding TitleFontSize}"
                    Text="{Binding TitleText}" />
            </StackPanel>
            <Border
                Name="BadgeView"
                Width="25"
                Height="25"
                Margin="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="{StaticResource BadgeColor}"
                CornerRadius="15"
                Visibility="{Binding IsBadgeVisible}">
                <TextBlock
                    Name="BadgeValue"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="White"
                    Text="{Binding BadgeText}" />
            </Border>
        </Grid>
        <VisualStateManager.VisualStateGroups />
    </Button>
</UserControl>
