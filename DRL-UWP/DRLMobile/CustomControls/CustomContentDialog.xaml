<ContentDialog
    x:Class="DRLMobile.CustomControls.CustomContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DRLMobile.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="dialog"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <!-- Content body -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" RowSpacing="8" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="title" Text="{Binding Title}" Margin="5" MaxLines="4" TextWrapping="Wrap" />
        <Button Grid.Row="1" Background="{StaticResource LoginButtonBackground}"  Content="{Binding FirstButtonText}" x:Name="btn1" Width="350" Click="btn1_Click" HorizontalAlignment="Center"  />
        <Button Grid.Row="2"  Background="{StaticResource LoginButtonBackground}" Content="{Binding FirstButtonText}" x:Name="btn2" Width="350"  Click="btn2_Click" HorizontalAlignment="Center"  />
        <Button Grid.Row="3"  Background="{StaticResource LoginButtonBackground}" Content="{Binding FirstButtonText}" x:Name="btn3" Width="350"  Click="btn3_Click" HorizontalAlignment="Center" />
    </Grid>
</ContentDialog>
